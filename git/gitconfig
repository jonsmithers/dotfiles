[alias]
  f = fetch
  w = website
  ad = add
  br = branch
  cm = commit
  co = checkout
  lg = log
  df = diff
  sh = show
  cp = cherry-pick
  dfs = diff --staged
  dfc = diff --check
  dfsc = diff --staged --check
  incoming = log ..@{u}
  outgoing = log @{u}..
  dfw = diff --word-diff=color
  lol = log --graph --decorate --oneline
  lola = log --graph --decorate --oneline --all
[user]
  name = Jon Smithers
  email = jon@smithers.dev
[difftool]
  prompt = false
[mergetool]
  keepBackup = false
[mergetool "diffconflicts"]
  cmd = vim -c DiffConflicts \"$MERGED\" \"$BASE\" \"$LOCAL\" \"$REMOTE\"
  trustExitCode = true
[branch]
  sort = -authordate
[rebase]
  autoStash = true
[commit]
  verbose = true
